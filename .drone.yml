kind: pipeline
name: default

trigger:
  branch:
    - dev-ravi
  event:
    - push
    - pull_request

steps:
- name: test
  image: node:latest
  commands:
  - cd server/
  - docker build -t mypublic .